# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
.next/
.turbo/
*.tsbuildinfo

# Env & secrets
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Test & coverage
coverage/
.nyc_output/

# Flutter / Dart (apps/mobile)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
apps/mobile/build/
apps/mobile/ios/Pods/
apps/mobile/ios/.symlinks/
apps/mobile/android/.gradle/
apps/mobile/android/local.properties
*.iml

# Python (services)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Editors & OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Claude Code local overrides (personal, not team-shared)
.claude/settings.local.json

# Local DB dump/backup files (never real data, but real backups can be
# large and are always machine-local -- see scripts/db-backup.mjs)
backups/

# task-registry's advisory lock directory (tools/task-registry/src/lock.ts,
# P0-011). Self-cleaning on a normal exit; only survives a hard crash, and
# even then is never something to commit.
tasks/.registry.yaml.lock/
