# Implementation Specifications

This directory contains small implementation contracts for concrete slices.

An implementation spec should state: scope, inputs/outputs, touched modules, contracts, tests, observability, security and rollback.

Large domains are split into several implementation specs instead of one long file.
