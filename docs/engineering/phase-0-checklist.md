# Phase 0 Checklist

- [ ] Git remote created and branch protection configured. Remote: done. Branch protection: exact settings documented in `docs/engineering/branch-protection.md`, not applied yet — needs GitHub repo-admin credentials no coding agent holds.
- [ ] Owner accounts and AI service identities separated. Human/org action (GitHub org membership, bot vs. human accounts) — not something a coding agent can execute; see `docs/engineering/branch-protection.md`'s note.
- [x] Dev/Stage/Prod environments defined. Config model (env vars, Doppler config mapping, promotion flow) in `docs/engineering/environments.md`. `stg`/`prd` are *defined*, not yet *provisioned* — hosting provider is a separate, still-open human decision (`docs/planning/phases/phase-0.md`'s "Human decisions").
- [x] Docker Compose local stack verified on developer machine. Real docker compose up + health-check cycle, P0-002.
- [x] PostgreSQL migration tooling selected. `node-pg-migrate`, `services/api/migrations/`; up/down/up verified against the live stack, see `docs/architecture/data-architecture.md`.
- [x] Seed data and synthetic family fixtures added. `packages/fixtures`, P0-006.
- [x] CI green on clean checkout. `.github/workflows/ci.yml`, P0-001.
- [ ] PR required checks enabled. Same blocker as branch protection above.
- [x] AI agent registry created. `docs/ai-team/agent-registry.yaml`.
- [x] Worktree allocation convention documented. `tools/task-registry` `worktree` subcommand + `docs/implementations/phase-0-agent-worktrees.md`, P0-004.
- [x] Task registry/dashboard chosen or built. `tools/task-registry` CLI, P0-003; `tasks/registry.yaml` as the machine-readable source of truth.
- [x] Architecture/QA/Security gates automated at least at baseline. CI runs lint/typecheck/test/build/gitleaks/audit/knip on every PR.
- [x] OpenAPI generation path established. `services/api/openapi/openapi.yaml` + `packages/api-client`, P0-005.
- [x] Observability baseline established. `packages/observability`, P0-007.
- [x] Backup/restore procedure tested in non-production. `pnpm db:backup`/`db:restore`, real drop-and-restore cycle verified against the live stack — see `docs/engineering/backup-dr.md`.
- [x] macOS build node plan created for iOS. GitHub-hosted `macos-latest` Actions runner (no dedicated Mac needed for CI-only unsigned builds); `.github/workflows/mobile-ios.yml`. See `docs/engineering/mobile-build-path.md` for the honest caveat: written and YAML-validated, not run end to end — no Flutter/Xcode toolchain available in this environment, and `apps/mobile` has no `ios/` platform folder committed yet.
- [x] Android internal build path created. `.github/workflows/mobile-android.yml`, unsigned debug APK as a CI artifact. `android/` is a real, committed platform folder, and **a debug APK was actually built and verified locally** (143 MB, `classes.dex` + `libflutter.so` for three ABIs). Two Windows path problems had to be fixed to get there — an 8.3-short-name temp dir breaking the JVM's `AF_UNIX` loopback socket, and AGP refusing the Cyrillic project path; both documented with the corrected diagnosis in `docs/engineering/mobile-build-path.md`. Neither affects CI. The workflow itself still has not been run on GitHub's runners (`workflow_dispatch`-only, needs a manual trigger).
- [x] Telegram/MAX/Alice integration sandboxes separated from production. Policy + token storage shape in `docs/integrations/sandbox-separation.md`. Creating the actual sandbox bot/skill accounts is a human step (needs a phone number/Yandex account) — the Human Architect has agreed to create these and hand over the tokens.
- [x] Contract versioning established. `contracts/registry.yaml` indexes every `domain-types`/OpenAPI contract group with version/owner/exports/consumers; `task-registry contracts validate` checks it against the real code both directions and runs in CI. P0-010, see `docs/architecture/contract-registry.md`.
- [x] AI task orchestration protocol implemented. `task-registry next` surfaces claimable work; `claim`/`handoff`/`block`/`unblock`/`reassign`/`add-discovery`/`create-child-task`/`close` are now all mutually exclusive via a real file lock (`tools/task-registry/src/lock.ts`) — a genuine two-process race on the same task was reproduced and fixed, not just theorized. Building the fix surfaced a second, unrelated real bug: Node's recursive `rmSync` silently failed to remove the lock directory on this repo's Cyrillic (`Работа`) path every time, with no exception — bisected to a bare Node script and fixed by using `unlinkSync`+`rmdirSync` instead. P0-011, see `docs/implementations/phase-0-ai-orchestration.md`.
- [x] Automated docs/task traceability checks. `scripts/check-docs-graph.mjs` (`pnpm run docs:check`, wired into CI) checks `docs/DOCS_GRAPH.md` against the real `docs/` tree both directions, and every `P<phase>-<NNN>` reference in docs/tasks against real `tasks/registry.yaml` ids. Run cold before any fix: 74 real problems (two dead references, 63 undocumented docs from six sections `DOCS_GRAPH.md` never learned about, six accumulated during Phase 0 itself) — fixed rather than loosening the check to tolerate them. `task-registry decisions` implements "Human Architect sees only unresolved decisions" directly: `*_BLOCKED` tasks, unresolved `human_decisions`, blocking `discovery_links`, nothing else. P0-012, see `docs/implementations/phase-0-docs-traceability.md`.
