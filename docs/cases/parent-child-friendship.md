# Case: Child Friendship With Parent Approval

## Path
Parent A friends Parent B → both approve → child friendship offered → each parent approves child pairing → child sees game-ready friend profile.

## Rules
No unrestricted child discovery. Parent graph and child graph remain separate.

## Success criteria
Both families can audit the connection and remove it.

## Safety
Block, report, communication policy, visibility policy and audit event required.
