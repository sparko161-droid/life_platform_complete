# Case: Success Case To Reusable Template

## Path
Family completes scenario → parent explicitly shares success case → moderation/privacy check → template extracted → catalog listing → other parent copies template → local customization.

## Rule
A family copy never mutates the global template.

## Metrics
Adoption, completion, customization rate, repeat usage, report rate.
