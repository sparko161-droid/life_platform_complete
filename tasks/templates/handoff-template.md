# Handoff — <TASK-ID> <Task title>

Generated by `tools/task-registry`'s `handoff` command (`pnpm task-registry -- handoff <id> ...`),
which also moves the task to `REVIEW` and records reviewer/gate owners in `tasks/registry.yaml`.
This file documents the fixed format so a handoff can also be written by hand when needed, per
`docs/ai-team/workflow.md` ("Use a fixed handoff format") and `AGENTS.md` ("Every agent reports...").

## Goal
What this task set out to do, one or two sentences.

## Branch
`agent/<role>/<task-id>-<slug>`

## Files changed
- path/to/file — why

## Contracts changed
- OpenAPI paths / domain types touched, with version note if applicable
- "none" if no contract changed

## Tests run
- command and result (e.g. `pnpm --filter <pkg> test` — pass)

## Known risks
- anything a reviewer should specifically look at

## Decisions
- decisions made while implementing, and why

## Next tasks
- follow-up task ids, or discoveries filed

## ADR required
State explicitly yes/no. If yes, link the ADR under `docs/adr/`.
