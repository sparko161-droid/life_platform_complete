# Handoff — P1-021 Independent Security Red Team capability and Phase 1 assessment

## Goal
Independent Security Red Team capability and Phase 1 assessment

## Branch
agent/security-red-team/p1-021-red-team-capability

## Files changed
- packages/security-red-team/package.json
- packages/security-red-team/tsconfig.json
- packages/security-red-team/src/finding.ts
- packages/security-red-team/src/findings.ts
- packages/security-red-team/src/index.ts
- packages/security-red-team/test/adversarial.test.ts
- tasks/registry.yaml (DISC-P1-021-1, DISC-P1-021-2 on P1-021)
- tasks/phase-1-blockers.yaml (BLK-P1-013 -> PARTIALLY_RESOLVED, BLK-P1-007 note)

## Contracts changed
- none

## Tests run
- 20 new node:test tests pass (packages/security-red-team/test/adversarial.test.ts)
- `pnpm run typecheck` / `pnpm run build`: clean
- `pnpm exec knip` (workspace-wide): clean
- `pnpm run contracts:validate`: clean (this package is not domain-types, no group needed)
- `node scripts/check-docs-graph.mjs`: clean
- `task-registry validate` / `task-registry control validate`: clean

## Known risks
- Every exploit attempt in test/adversarial.test.ts runs against the real
  @life/domain-types package, not a mock -- 16 real attempts, 12 blocked by
  a verified control, 4 succeeded (real gaps). This is not a hypothetical
  assessment.
- 4 findings are ACCEPTED_RISK, not BLOCKED: RT-002/RT-003/RT-005/RT-016
  (actor/family-membership authorization is not enforced by task-service.ts
  or reward-service.ts -- confirmed exploitable, including RT-003 CRITICAL:
  a child can self-approve their own submitted task) and RT-010 (optimistic
  version checks exist but nothing calls them, so concurrent writes are not
  actually prevented today). All five are recorded as DISC-P1-021-1/2 on
  this task and as a note on BLK-P1-007, with retestResult=NOT_RETESTED
  because there is no application/persistence layer yet to retest against.
  These MUST be retested once BLK-P1-007 lands -- they are acceptance
  criteria for that blocker's resolution, not optional hardening.

## Decisions
- BLK-P1-013 narrowed to PARTIALLY_RESOLVED, not RESOLVED: the *capability*
  (a real, re-runnable adversarial test harness) is built and has been
  exercised, which is what the blocker's `blocks: [W3, W4, W7, W8,
  phase-1-exit]` waves actually needed to stop waiting on. "require retest
  after fixes" cannot fully close until the 4 ACCEPTED_RISK findings are
  retested against a real implementation.
- Findings are structured data (FindingSchema, zod-validated, one category
  enum, one status enum, one retestResult enum) rather than prose, matching
  this repo's existing CONFLICT_SCENARIOS/IDEMPOTENCY_RULES pattern -- a
  future re-run can diff results against this file instead of a human
  re-reading a report.

## Next tasks
- BLK-P1-007 (transactional persistence and real API path) must retest
  RT-002/RT-003/RT-005/RT-010/RT-016 once it lands.

## ADR required
No — this is an assessment and a reusable test harness against existing
architecture, not a new foundational decision. The authorization gap it
found is a requirement on BLK-P1-007's design, tracked there.
